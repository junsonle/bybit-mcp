{
  "mcpServers": {
    "bybit": {
      "command": "python",
      "args": ["-c", "import nest_asyncio; nest_asyncio.apply(); from deploy import mcp; import asyncio; asyncio.run(mcp.run_async(transport='sse', host='0.0.0.0', port=8081))"]
    }
  }
}
